<?xml version="1.0" encoding="utf8"?>
	<packages>
		<!-- here's the format:
		<package id="" version="" source="" /> -->
		<!-- todo: YOU CAN'T JUST UNISTALL THINGS FROM HERE, YOU HAVE TO ALSO CHOCO UNINSTALL THEM OR THEY WILL REINSTALL NEXT TIME -->

		<package id="7zip" />
		<package id="autohotkey" />
        	<package id="ack" />
		<!--package id="ActivePerl" no need for perl anymore/-->
		<package id="directx" />
		<package id="dotnet3.5" />
		<!-- package id="dropbox" not wanted on laptop now, so desktop you'll have to update manaully/-->
		<package id="firefox" />
		<!--package id="freefilesync" - been broken for 2yrs+ - still on v6.9 https://chocolatey.org/packages/freefilesync /-->
		<package id="foxitreader" />
		<package id="git" params="/GitAndUnixToolsOnPath" />
		<package id="GoogleChrome" />
		<!--package id="glasswire" - network monitor but it refuses to stop update prompts/-->
		<!--package id="googledrive" not wanted on laptop now, so desktop you'll hae to update manually /-->
        	<!--package id="iCloud" why bother, its only taking up resources/-->
		<package id="jbs" />
		<!--package id="jdk" doesn't exist anymore closes i could find is below/-->
		<package id="ojdkbuild" />
		<!-- keyboard-layout-creator - 1.4 is the right version but the install is borked-->
		<package id="linkshellextension" />
		<!-- package id="listary" started taking mouse control from joy2key. last straw/-->
		<package id="lili" />
		<!-- package id="netdrive" currently this installs netdrive 3, which has no command line support, sticking with net drive 2/-->
		<package id="netdrive" version="2.6.12.928" />
		<!--you might think choco update will install netdrive 3 over the top of 2 but no see https://stackoverflow.com/a/26870291 -->
		<package id="notepadplusplus" />
		<package id="qbittorrent" />
		<package id="rufus" />
		<!--package id="skype" shipped with windows now, unavoidable/-->
		<!--package id="Spotify" ships with windows now/-->
		<!--package id="slack" not right now/-->
		<!--package id="SourceTree" never use it/-->
		<!--package id="teamviewer" not anymore/-->
		<package id="steam-client" />
		<package id="vcredist2010" />
		<package id="vcredist2013" />
		<package id="vcredist2015" />
		<package id="velocity" />
		<package id="vim-x64" />
		<package id="virtualbox" />
		<!--package id="virtualbox.ExtensionPack" VirtualBox.ExtensionPack package has been obsoleted in favor of having both VirtualBox and the extension pack installed via the same package - the "virtualbox" package /-->
		<!--package id="virtualbox-Guest-Additions-guest.install" This package should be installed inside your guest Windows VM and is useful for automating the building of Virtualbox VM's. It is not meant to be installed on the host. /-->
		<package id="windirstat" />
		<package id="winmerge" />
		<!--why nvm portable version? its at the latest version, the non portable version is not /-->
		<package id="nvm.portable" />

	</packages>
